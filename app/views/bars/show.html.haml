%h1= bar.name
%p
  City:
  = bar.lat
%p
  Street:
  = bar.lng
/ map div
#map{:style => "width: 550px; height: 350px;"}

:javascript
  $(document).ready(function(){
    var local = #{bar.to_json}    // pass ruby variable to javascript
    gmap_show(local);    // init show map for local card (only print marker)
  });